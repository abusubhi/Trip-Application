@model List<StepTrips.DataContract.DTOs_Trip.TripDTO>
@using StepTrips.DataContract.Model;

<table class="table table-striped table-hover table-bordered" style="width:100%">
    <thead>
        <tr>

            <th>
                #
            </th>
            <th>
                Full Name
            </th>
            <th>
                Vacation In Term
            </th>
            <th>
                Sick Days
            </th>
            <th>
                Take Vacation IN
            </th>
            <th>
                Action
            </th>
        </tr>

    </thead>
    @* <tbody>
        @if (Model != null)
        {
            @foreach (var item in Model)
        {

                <tr>
                    <td>
                        @item.Id
                        @Html.DisplayFor(test => Model)
                    </td>
                    <td>
                        @Html.DisplayFor(model => Model)
                    </td>
                    <td>
                        @Html.DisplayFor(modelitem => Model)
                    </td>
                    <td>
                        @Html.DisplayFor(modelitem => Model)
                    </td>
                    <td>
                        @foreach (var item in Model)
                        {
                            @Html.DisplayFor(modelitem => Model)
                            <p>&&</p>
                        }
                    </td>
                    <td>
                        <a class="btn btn-primary" asp-action="VacationDetails" asp-route-id="@Model.Id">Details </a>
                        <a class="btn btn-dark" asp-action="CreateVacations" asp-route-id="@Model.Id">Create </a>


                    </td>
                </tr>

            }
        }
    </tbody> *@
</table>

